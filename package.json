{
  "name": "residual-worlds",
  "private": true,
  "version": "0.1.0",
  "description": "Repository root: the study lives in src/residual_worlds/, the site in site/.",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=22.22"
  },
  "scripts": {
    "go": "pnpm --dir site install && pnpm --dir site run build && pnpm --dir site run preview",
    "dev": "pnpm --dir site install && pnpm --dir site run dev",
    "check": "pnpm --dir site install && pnpm --dir site run check"
  }
}
